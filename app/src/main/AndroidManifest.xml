<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.hallie.diet">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Record" android:label="@string/title_record"/>
        <activity android:name=".Myinfo_Q" />
        <activity android:name=".Myinfo_A" />
        <activity
            android:name=".Mealtype"
            android:label="@string/title_mealtype"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Setup" />

    </application>

</manifest>